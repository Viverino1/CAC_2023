type Task = {
start: number,
end: number,
title: string,
description: string,
}

type Status = "Unstarted" | "In Progress:" | "Complete";

type Product = {
  name: string,
  costToMake: number,
  sellingProce: number,
  stock: number,
}
